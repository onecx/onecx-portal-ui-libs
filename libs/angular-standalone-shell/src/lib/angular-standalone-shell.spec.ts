import { angularStandaloneShell } from './angular-standalone-shell'

describe('angularStandaloneShell', () => {
  it('should work', () => {
    expect(angularStandaloneShell()).toEqual('angular-standalone-shell')
  })
})
