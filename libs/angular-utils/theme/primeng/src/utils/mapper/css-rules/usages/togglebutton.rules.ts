import type { CssRule } from '../../mapper.types'

export const togglebuttonCssRules: CssRule[] = [
  {
    selector: '.p-togglebutton',
    declarations: [{ property: 'font-size', from: 'usages.togglebutton.font.size' }],
  },
]
