import type { CssRule } from '../../../mapper.types';

/**
 * CSS rules for tabs navigation buttons.
 * NavButton tokens from v1 schema only define icon properties (nextIcon, prevIcon),
 * which are handled via mapping rules. No additional CSS rules are needed at this time.
 */
export const navbuttonRules: CssRule[] = [];