import type { MappingRule } from '../../mapper.types';

export const diagramMappingRules: MappingRule[] = [
    {
        from: 'usages.diagram.selectButton.borderRadius',
        to: 'components.selectbutton.root.borderRadius'
    }
]