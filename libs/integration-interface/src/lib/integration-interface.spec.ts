import { integrationInterface } from './integration-interface'

describe('integrationInterface', () => {
  it('should work', () => {
    expect(integrationInterface()).toEqual('integration-interface')
  })
})
