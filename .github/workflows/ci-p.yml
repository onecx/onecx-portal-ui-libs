name: On Push

on:
  push:
    branches:
      - '*'
      - '*/*'
      - '**'

jobs:
  main:
    uses: ./.github/workflows/ci-common.yml
    secrets: inherit

  sonar:
    needs: main
    uses: ./.github/workflows/sonar.yml
    secrets: inherit
